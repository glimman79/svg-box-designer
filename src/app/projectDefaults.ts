/** Central defaults shared by project state and manufacturing UI. */
export const DEFAULT_PROJECT_SETTINGS = Object.freeze({
  kerfMm: 0.15,
  profileOffsetMm: 0.00,
  slotClearanceMm: -0.10,
  selectedProfileOffsetIds: Object.freeze([]),
  tapClearanceMm: -0.10,
  selectedTapIds: Object.freeze([]),
});
